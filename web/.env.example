# Development environment variables (web/.env.example)
# Copy this file to .env.development and fill in actual values
# .env.development is gitignored - DO NOT commit secrets

# Backend agent URL - points to local ADK server
AGENT_URL=http://localhost:8000

# =============================================================================
# CopilotKit Configuration
# =============================================================================
# Public key for CopilotKit (free for personal use)
# Get yours at: https://cloud.copilotkit.ai
NEXT_PUBLIC_COPILOTKIT_PUBLIC_KEY=<your-copilotkit-public-key>

# Environment identifier (used for conditional logic if needed)
NEXT_PUBLIC_ENVIRONMENT=development

# =============================================================================
# Better Auth Configuration
# =============================================================================

# Base URL for auth callbacks and redirects
BETTER_AUTH_URL=http://localhost:3000

# Auth secret for session encryption (generate with: openssl rand -base64 32)
BETTER_AUTH_SECRET=<generate-a-secret>

# =============================================================================
# Mailgun Configuration (Email Verification)
# =============================================================================

# Mailgun API key (from Mailgun dashboard)
MAILGUN_API_KEY=<your-mailgun-api-key>

# Mailgun domain (EU region)
MAILGUN_DOMAIN=mail.example.com

# From address for emails
MAILGUN_FROM=Knowsee <noreply@mail.example.com>

# =============================================================================
# Database (Local Postgres via Docker)
# =============================================================================
# Start with: make db-up
DATABASE_URL=postgresql://knowsee:localdev@localhost:5432/knowsee

# =============================================================================
# E2E Testing (Playwright)
# =============================================================================
# Pre-verified test user for authenticated flow tests
# Create a user manually, verify email, then add credentials here
E2E_TEST_USER_EMAIL=<your-test-user-email>
E2E_TEST_USER_PASSWORD=<your-test-user-password>

# =============================================================================
# Production Configuration (Cloud Run)
# =============================================================================
# When deploying to production, set these environment variables:
#
# AGENT_URL=https://backend-service-xxx.run.app
# BETTER_AUTH_URL=https://your-production-domain.com
# BETTER_AUTH_SECRET=<from Secret Manager>
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
# MAILGUN_API_KEY=<from Secret Manager>
