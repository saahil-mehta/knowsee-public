# Development environment variables (sagent/.env.example)
# Copy this file to .env.development and fill in actual values
# .env.development is gitignored - DO NOT commit secrets

# GCP Configuration
GOOGLE_CLOUD_PROJECT=sagent-development
GOOGLE_CLOUD_LOCATION=global
GOOGLE_GENAI_USE_VERTEXAI=TRUE

# Environment (development | production)
# Controls artifact storage: development=InMemory, production=GCS
ENVIRONMENT=development

# =============================================================================
# Database (Local Postgres via Docker)
# =============================================================================
# Start with: make db-up
DATABASE_URL=postgresql://knowsee:localdev@localhost:5432/knowsee

# =============================================================================
# RAG Configuration (Team-based access via database)
# =============================================================================
# Team corpora are managed via the API (/api/corpora endpoints) and stored in
# the database. Users can only search corpora their teams have access to.
#
# RAG retrieval tuning parameters (defaults shown):
# RAG_SIMILARITY_TOP_K=10
#
# Team membership provider (default: better_auth)
# TEAM_MEMBERSHIP_PROVIDER=better_auth  # Options: better_auth, google_groups, azure_ad

# =============================================================================
# Production Configuration
# =============================================================================
# When deploying to production, set these environment variables:
#
# GOOGLE_CLOUD_PROJECT=<production-project-id>
# GOOGLE_CLOUD_LOCATION=global
# GOOGLE_GENAI_USE_VERTEXAI=TRUE
# ENVIRONMENT=production
# ARTIFACT_BUCKET=knowsee-artifacts-prod
# DATABASE_URL=postgresql://user:pass@/dbname?host=/cloudsql/project:region:instance
